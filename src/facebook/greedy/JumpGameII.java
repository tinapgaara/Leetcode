package facebook.greedy;

/**
 * Created by yingtan on 2/19/18.
 *
 * The main idea is based on greedy. Let’s say the range of the current jump is [curBegin, curEnd], curFarthest is the farthest point that all points in [curBegin, curEnd] can reach. Once the current point reaches curEnd,
 * then trigger another jump, and set the new curEnd with curFarthest,
 * then keep the above steps, as the following:
 */
public class JumpGameII {
}
